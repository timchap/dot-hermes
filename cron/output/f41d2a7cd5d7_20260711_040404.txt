Cronjob Response: session-audit-nightly
(job_id: f41d2a7cd5d7)
-------------

Now I have all the data I need. Let me compile the report.

---

## Security Risk Assessment

**Overall: LOW RISK — No critical issues found.** The past 24 hours of sessions show mostly routine infrastructure work. Here are the specific findings:

### Observations (non-issues, but worth noting):

1. **`/usr/local/bin/restic-backup.sh` created as root (755)** — This script runs as root and uses `GOOGLE_APPLICATION_CREDENTIALS` pointing to `/root/.config/restic/gcs-credentials.json`. The script has full filesystem access (`backup /`). However:
   - The GCS credentials file **does not yet exist** on the Pi (GCP Phase 0 hasn't been run). The script is blocked until the key is provisioned, so no active credential exposure risk.
   - File permissions are 755 — while the script itself is benign, it would be better practice to lock it to 700 root since it handles secrets.

2. **`hermes-config-watcher.service` systemd unit** — Created and running. Security posture:
   - Runs as `User=hermes` (not root) ✅
   - Watches `~/.hermes` for file changes and auto-commits+pushes to GitHub ✅
   - Excludes noisy/unsafe dirs (cache/, logs/, .git, kanban/, sessions/) ✅
   - Uses `flock` for commit coalescing to prevent race conditions ✅
   - **Note**: If a secret file is ever placed in a watched directory (e.g., `skills/`), the watcher will auto-commit it to the git repo. The `.gitignore` excludes `.db-wal`, `.db-shm`, `*_cache.json`, `state.db`, `session/`, and volatile files, but there's no explicit protection against secrets (`.env`, API keys, etc.). This is a medium-priority improvement to add.

3. **GCS service account key tasks blocked** — Multiple kanban sessions (`t_76f93989`) correctly identified and reported that no GCS key exists on the Pi. No credentials were written anywhere. Good.

4. **No firewall changes, user modifications, or untrusted package installs** detected in any session.

5. **No subagent delegations** performed broad filesystem or network operations — all work was done within direct tool calls.

---

## Documentation Gaps

### Homelab repo (`~/homelab/`)

**Recent commit (past 24h):**
- `ddeb328 fix: hf cache` — Updated `ansible/roles/lemonade/tasks/configure.yml`, `ansible/roles/lemonade/tasks/fedora.yml`, and `docs/lemonade.md`. **✅ Documented and committed+pushed.**

**Missing documentation:**

| Change | Doc Update | Git Commit |
|--------|-----------|------------|
| `restic-backup.sh` created at `/usr/local/bin/` (kanban task t_039a06b9) | ❌ Not documented in `docs/backup-restore.md` or `docs/hermes.md` | ❌ Not committed (file is on disk, not in repo) |
| `hermes-config-watcher.service` systemd service created in `/etc/systemd/system/` | ❌ Not documented in `docs/hermes.md` or any runbook | ❌ Not committed (systemd unit file not tracked in homelab repo) |
| `hermes-config-watcher.sh` script in `~/.hermes/bin/` | ❌ Not documented | ✅ Committed to `~/.hermes` dot-hermes repo |

### Dot-hermes repo (`~/.hermes/`)

- 30 auto-commits from the watcher over the past 24h. All committed+pushed to `github.com:timchap/dot-hermes.git`. **✅ In order.**
- Untracked files present: `.config-watcher.*`, `bin/tirith`, `bin/uv`, `bin/uvx`, `hermes-agent/`, `lsp/`, `processes.json`, `TODO.md` — these are tooling caches and setup artifacts, not secrets.

### Summary

**Two changes lack homelab documentation:**
1. **Restic backup script** — Deployed but not reflected in `docs/backup-restore.md`. The doc has a reference script template but doesn't note the actual deployed script at `/usr/local/bin/restic-backup.sh` or the systemd timer (if one was created alongside it — I don't see one, which is itself a gap).
2. **Config watcher service** — Not documented anywhere in the homelab docs. It's a new always-on systemd service that watches config files and auto-commits/pushes — this should be in `docs/hermes.md` or a new runbook.

To stop or manage this job, send me a new message (e.g. "stop reminder session-audit-nightly").